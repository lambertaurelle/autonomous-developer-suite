# Production-grade Python code standards fallback
line-length = 100
target-version = "py310"

[lint]
select = [
  "E",   # pycodestyle errors
  "F",   # Pyflakes
  "W",   # pycodestyle warnings
  "C90", # mccabe complexity
  "I"    # isort imports
]
ignore = []

[lint.mccabe]
max-complexity = 10
